You can compile a program to read bin files write the hash function ouptut of the random numbers from the input bin to and output bin.

To compile for each hash function:
./make generic1
./make univeral
./make zobrist

The program name will be ${hash_function}_hash_numbers

